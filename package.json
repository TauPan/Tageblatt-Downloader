{
  "scripts": {
    "download": "testcafe chromium --hostname localhost --skip-js-errors --debug-on-fail",
    "download-headless": "testcafe chromium:headless --hostname localhost --skip-js-errors --screenshots screenshots --screenshots-on-fails"
  },
  "dependencies": {
    "testcafe": "^0.22.0",
    "testcafe-live": "^0.1.3"
  }
}
