{
  "scripts": {
    "download": "testcafe chrome --hostname localhost --skip-js-errors --debug-on-fail test/test_*",
    "download-headless": "testcafe chrome:headless --hostname localhost --skip-js-errors --screenshots screenshots --screenshots-on-fails  test/test_*"
  },
  "dependencies": {
    "testcafe": "*"
  },
  "resolutions": {
    "useragent": "^2.1.13"
  }
}
